import Ember from 'ember';
import AuthorizedRouteMixinMixin from '../../../mixins/authorized-route-mixin';
import { module, test } from 'qunit';

module('Unit | Mixin | authorized route mixin');

// Replace this with your real tests.
test('it works', function(assert) {
  var AuthorizedRouteMixinObject = Ember.Object.extend(AuthorizedRouteMixinMixin);
  var subject = AuthorizedRouteMixinObject.create();
  assert.ok(subject);
});
